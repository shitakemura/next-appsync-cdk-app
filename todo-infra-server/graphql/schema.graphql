scalar AWSTimestamp

type Todo {
  id: ID!
  userId: String!
  title: String!
  completed: Boolean!
  createdAt: AWSTimestamp!
}

input AddTodoInput {
  title: String!
}

type Query {
  getTodos: [Todo!]!
}

type Mutation {
  addTodo(addTodoInput: AddTodoInput!): Todo
}
